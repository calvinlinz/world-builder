variables:
  MAVEN_OPTS: -Dmaven.repo.local=.m2/repository

image: maven:latest

stages:
  - build

build-docker:
  stage: build
  tags: 
    - shell
  services:
    - docker:dind
  script:
    - mvn --version
    - sudo chmod +x /local/docker/100000.100000/buildkit/executor/mhiv14n9uoarizci1bex5t5si/rootfs
    - echo "This is a shell script command"
    - ./deploy.sh
